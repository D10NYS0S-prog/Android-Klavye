<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.turkceklavyem">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TurkceKlavyem">
        
        <!-- Ana aktivite - Klavye kurulum rehberi -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Ayarlar aktivitesi -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="Klavye Ayarları"
            android:parentActivityName=".MainActivity" />
        
        <!-- Sözlük içe aktarma aktivitesi -->
        <activity
            android:name=".DictionaryImportActivity"
            android:exported="false"
            android:label="Sözlük İçe Aktar"
            android:parentActivityName=".MainActivity" />
        
        <!-- Klavye servisi -->
        <service
            android:name=".T9KeyboardService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:label="@string/keyboard_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
</manifest>